program = { SOI ~ instruction* ~ EOIS }

instruction = { add }

add      =  { "add" ~ register ~ register ~ register }
register = ${ "x" ~ NUMBER }

WHITESPACE = _{ " " | "\t" | NEWLINE }
COMMENT    = _{ ("//" | "#") ~ (!NEWLINE ~ ANY)* }
EOIS       = _{ !ANY }
